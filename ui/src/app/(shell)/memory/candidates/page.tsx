"use client";

import { MemoryCandidatesScreen } from "@/components/screens/memory/MemoryCandidatesScreen";

/** 记忆 — 候选（/api/memory/candidates 投影；auto_commit 机制如实说明） */
export default function MemoryCandidatesPage() {
  return <MemoryCandidatesScreen />;
}
