# ── Environment ──
.env
.env.*
!.env.example

# ── Python ──
.venv/
venv/
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# ── Node / UI ──
node_modules/

# ── macOS ──
.DS_Store
Thumbs.db

# ── Logs & runtime artifacts ──
*.log
*.pid
*.lock
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite3
runtime/

# ── Test & coverage ──
coverage.xml
htmlcov/
.coverage
.pytest_cache/

# ── Runtime data ──
runtime-data/
workspace/tmp/
workspace/cache/
evidence/raw/

# ── Reports tmp / draft ──
reports/tmp/
reports/acceptance-*/
reports/production_hardening/
!reports/production_hardening/hardening_acceptance_report.md

# ── Obsidian transient ──
.obsidian/
docs/.obsidian/workspace*

# ── Trash (legacy purged notes) ──
docs/.trash/

# ── NEXARA state (tracked separately via .nexara/ in git) ──
# .nexara/ is intentionally tracked — it holds project state
