name: Runner Diagnostic

on:
  push:
    branches:
      - diagnostics/github-actions-runner-20260715
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Runner assigned
        run: |
          echo "RUNNER_OK"
          uname -a
