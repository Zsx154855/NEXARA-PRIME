name: CI Smoke Test
on:
  push:
    branches: ["work/*"]
permissions:
  contents: read
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Runner OK" && ls -la && python3 --version
